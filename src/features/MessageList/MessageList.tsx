import { FC } from "react";
import * as Styled from "./MessageList.styles";

interface IProps {}

export const MessageList: FC<IProps> = () => {
  return <Styled.MessageList>list</Styled.MessageList>;
};
